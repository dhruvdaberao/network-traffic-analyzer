# Sample Data Notes

This project uses an internal demo traffic generator instead of static PCAP files so the complete capture, parsing, detection, persistence, and dashboard pipeline behaves the same way in presentations.

Demo traffic includes:

- Normal HTTP browsing
- HTTPS/TLS sessions
- DNS lookups
- Download burst traffic
- ICMP sequences
- Port scan style SYN probes
- Repeated DNS request patterns
